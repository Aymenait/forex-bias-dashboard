# Redirect the old netlify subdomain to the new custom domain
https://marketalgeriaa.netlify.app/*  https://marketalgeria.store/:splat  301!

# Perplexity is no longer offered; keep old shared/search links from opening an order page.
/perplexity_landing      /                         301!
/perplexity_landing.html /                         301!

# Ensure extensionless URLs work (Cloudflare handles this by default, but good to have)
# /page -> /page.html
